# MoonXZ 项目申报书

**基本信息**

- 项目名称：MoonXZ：MoonBit 原生 XZ / LZMA / LZMA2 压缩工具包
- 参赛者：lin2077-zeming
- GitHub 仓库：https://github.com/lin2077-zeming/MoonXZ
- 项目方向：MoonBit 压缩与归档基础库 / 跨平台数据编解码
- 项目类型：原创实现，参考公开格式规范及已有开源实现

**项目简介**

MoonXZ 为 MoonBit 生态补充 `.xz` 与旧格式 `.lzma` 的读写与校验能力。纯 MoonBit
实现 XZ 容器、LZMA/LZMA2 编解码、CRC32/CRC64/SHA-256，不依赖 C 库或平台 FFI，
可运行于 wasm、wasm-gc、js、native。当前 `0.4.1` 已发布至 mooncakes.io：解压端
可处理 Python `lzma`、`xz-utils` 等标准工具生成的数据，支持多 block、拼接
stream、stream padding；编码端具备 range encoder、hash chain match finder、
压缩级别 0-9 与字典大小参数，并能写出 `.lzma`。

**项目方向与通用性**

XZ 是软件分发、系统工具、日志归档与数据交换中的常见格式。MoonBit 生态已有
DEFLATE、gzip、zlib、ZIP、Zstandard、Brotli，但缺少 XZ/LZMA 支持。MoonXZ 接口
以 `Bytes` 与一次性 API 为主，核心逻辑不依赖文件系统与运行时环境，因此既可用于
原生工具，也可用于 WASM/JS 应用与库作者的工具链。

**预期使用场景**

1. **构建与包管理工具**：下载 `.xz` 形式的源码包或二进制包后，先验证
   CRC64/SHA-256 再解压并写入缓存，全程在 MoonBit 内完成，不引入平台解压程序。
2. **WASM/JS 数据管线**：浏览器或边缘函数接收 `.xz` 静态资源、日志块、数据集，
   在 wasm-gc/js 目标直接解压，并用输出大小限制避免异常数据造成内存膨胀。
3. **备份与日志归档 CLI**：批量处理日志或备份文件，以 XZ 格式存储，解压时校验
   block check，并通过流式 Reader/Writer 按 block 处理，避免一次性载入全部内容。

**交付范围与工程边界**

已交付：XZ stream/block/index/footer 解析与校验，LZMA2 压缩块与未压缩块解码，
拼接 stream、stream padding、输出大小限制；LZMA2 range encoder、hash chain match
finder、压缩级别 0-9、字典大小参数、压缩不划算时自动回退未压缩块；`.lzma` 双向
读写（含已声明大小与 EOS 标记两种终止方式）；CRC32/CRC64/SHA-256、delta 与 x86
两个 filter；流式 `XzReader`/`XzWriter`、文件 CLI、四目标 CI、34 个测试。

**明确不做**：

- **只交付 delta 与 x86 两个 filter。** IA64、ARM、ARM64、ARM-Thumb、PowerPC、
  SPARC 六个 filter id 一律返回 `Unsupported`。其中 ARM、ARM64、SPARC 曾实现并
  通过初步向量测试，但在"多长度 × 多内容形态"矩阵下被判定会静默损坏数据：地址
  运算在特定字上溢出后编码解码不再互逆，`liblzma` 会拒绝其输出。缺少可靠对照
  实现时，宁可明确报错，也不交付"大多数情况下正确"的位域实现。
- LZMA1 只支持 `.lzma` 容器，不提供独立 LZMA1 raw stream API；
- 不提供逐字节增量接口，流式 API 以 block 为单位；
- 不追求压缩率对齐：项目当前只生成 literal 与普通 match，不实现 optimal parse。

**实现路径与技术理解**

路线是"解码优先、编码渐进"：先把 XZ 容器与 LZMA2 解码做正确，再补未压缩块写入，
随后加入 range encoder 与 match finder，最后扩展到 filter、流式 API 与 `.lzma`。
三条贯穿全程的判断：

- **纯 MoonBit 核心**：编解码核心只依赖 `Bytes`，四个目标行为一致；文件读写隔离
  在 CLI 包，不污染库核心。
- **正确性优先于覆盖率**：filter 位域算错会静默产出错误字节，block check 只能
  发现不一致、无法区分"正确"与"看起来正确"，因此每个 filter 都要求与 `liblzma`
  双向互通，而不是只做单向向量比对。
- **测试维度必须同时覆盖长度与内容形态**：三个被撤回的 filter 有同一失效模式——
  把"像分支的字"做无符号加减而不检查结果是否仍在字段宽度内；它们躲过初版测试的
  原因是那些测试只用单一重复模式配单一长度。现在矩阵测试遍历 4 个 filter ×
  19 个长度 × 5 种内容形态，另有逐字节逐位翻转的损坏输入穷举测试（已借此发现
  重叠 match 越界与 LZMA2 框架缺少交叉校验两处缺陷）。

**原创与参考说明**

本项目是原创 MoonBit 实现，不是直接移植。开发中参考 XZ file format specification
与纯 Go 实现 `github.com/ulikunitz/xz` 的算法设计，用于理解 LZMA 状态机、距离解码
与概率模型；参考项目采用 BSD-3-Clause，来源 https://github.com/ulikunitz/xz 。
项目保留其版权与许可证说明，但未逐行复制 Go 源码。各 filter 与 `.lzma` 的行为以
Python 3.14 `liblzma` 生成的数据交叉验证。MoonXZ 自身采用 Apache-2.0。
