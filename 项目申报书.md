# MoonXZ 项目申报书

**基本信息**

- 项目名称：MoonXZ：MoonBit 原生 XZ/LZMA2 压缩工具包
- 参赛者：lin2077-zeming
- GitHub 仓库：https://github.com/lin2077-zeming/MoonXZ
- 项目方向：MoonBit 压缩与归档基础库 / 跨平台数据编解码
- 项目类型：原创实现，参考公开格式规范及已有开源实现

**项目简介**

MoonXZ 为 MoonBit 生态补充 `.xz` 格式的读取、写入和校验能力。项目使用纯 MoonBit 实现 XZ 容器、LZMA2 解码、CRC32、CRC64 和 SHA-256，不依赖 C 库或平台 FFI，可运行于 wasm、wasm-gc、js 和 native 目标。当前版本已经能够解压由 Python `lzma`、`xz-utils` 等标准工具生成的压缩流，支持多 block、拼接 stream 和 stream padding；编码端先提供规范兼容的未压缩 LZMA2 块，保证与其他实现的互操作性。后续将在现有容器和 API 基础上加入 range encoder、match finder 和可调压缩级别，使其成为 MoonBit 中可长期复用的压缩基础库。

**项目方向与通用性**

XZ 是软件分发、系统工具、日志归档和数据交换中常见的压缩格式。MoonBit 生态已有 DEFLATE、gzip、zlib、ZIP、Zstandard 和 Brotli 等实现，但缺少 XZ/LZMA2 支持。MoonXZ 的接口以 `Bytes` 和一次性 API 为主，核心逻辑不依赖文件系统和运行时环境，因此既能用于 MoonBit 原生工具，也能用于 WASM/JS 应用和库作者的工具链。

**预期使用场景**

1. **构建与包管理工具**：工具下载 `.xz` 形式的源码包或二进制包后，先验证 CRC64/SHA-256，再调用 MoonXZ 解压并写入缓存。整个流程可在 MoonBit 内完成，不引入平台相关的解压程序。
2. **WASM/JS 数据管线**：浏览器应用或边缘计算函数接收 `.xz` 静态资源、日志块或数据集，在 wasm-gc/js 目标中直接解压，并通过输出大小限制避免异常数据造成内存膨胀。
3. **备份与日志归档 CLI**：命令行工具批量处理日志或备份文件，使用 XZ 格式存储，解压时校验 block check，并在未来通过流式 API 处理大文件，避免一次性载入全部内容。

**拟实现的核心功能**

- 已完成：XZ stream、block、index、footer 解析与校验，LZMA2 压缩块和未压缩块解码，CRC32、CRC64、SHA-256，拼接 stream 和输出限制；
- 已完成：规范兼容的存储式 XZ 写入、CLI 示例、Python `lzma` 双向互操作测试和四目标 CI；
- 计划实现：LZMA2 range encoder、hash chain match finder、压缩级别和字典大小参数；
- 计划实现：流式 Reader/Writer、文件 CLI、delta filter 和 x86 BCJ filter；
- 计划实现：与 `xz-utils` 的编码结果对照测试、压缩率基准、损坏输入测试集，并发布至 mooncakes.io。

**原创与参考说明**

本项目是原创 MoonBit 实现，不是对现有项目的直接移植。开发过程中参考了 XZ file format specification，以及纯 Go 实现 `github.com/ulikunitz/xz` 的算法设计，用于理解 LZMA 状态机、距离解码和概率模型。参考项目采用 BSD-3-Clause 许可证，来源为 https://github.com/ulikunitz/xz；项目保留其版权和许可证说明，但没有逐行复制 Go 源码。MoonXZ 自身采用 Apache-2.0 许可证。

仓库已包含分阶段开发记录、跨平台测试和持续集成配置，当前有效提交数不少于 10 个。
